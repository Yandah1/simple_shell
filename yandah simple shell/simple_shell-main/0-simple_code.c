#include "shell.h"
#include <stdio.h>

/**
 * sub_numbers - Subtracts two numbers
 * @a: first number
 * @b: second number
 *
 * Return: The difference between the two numbers
 */
int sub_numbers(int a, int b)
{
	return (a - b);
}
